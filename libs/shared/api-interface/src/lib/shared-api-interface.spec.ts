import { sharedApiInterface } from './shared-api-interface';

describe('sharedApiInterface', () => {
  it('should work', () => {
    expect(sharedApiInterface()).toEqual('shared-api-interface');
  });
});
